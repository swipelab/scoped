name: scoped
description: a pragmatic state management solution. comes with  instance store (single, lazy & and scoped) and handy connectors
version: 0.6.1
homepage: http://github.com/agrapine/scoped
authors:
  - Alexandru R. Agrapine <alexandru@agrapine.com>
  - Basile Raynaud <b.c.j.raynaud@gmail.com>

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
